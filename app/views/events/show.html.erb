<h1><%= @event.title %> </h1>
<p>created by <%= User.find_by(id: @event.creator_id).name %> <p> 

<h3> Attendees </h3>
<ul>
  <% @event.attendees.each do |attendee| %>
    <li><%= attendee.name %></li>
  <% end %>
</ul>